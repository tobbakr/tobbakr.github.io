---
layout: single-page
image: assets/img/social_schrijnwerkerijtobback.png
---
{% include breadcrumbs.liquid %}

<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Service",
    "name": "{{ page.title }}",
    "description": "{{ page.description }}",
    "provider": {
      "@type": "HomeAndConstructionBusiness",
      "name": "Schrijnwerkerij Tobback",
      "url": "https://schrijnwerkerijtobback.be"
    },
    "areaServed": {
      "@type": "AdministrativeArea",
      "name": "Vlaams-Brabant"
    },
    "serviceType": "{{ page.category }}"
  }
</script>

<section>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-8">
        {{ content }}
      </div>
    </div>
  </div>
</section>

{% assign diensten_sorted = site.diensten | where_exp: 'item', 'item.url != page.url' | sort: 'importance' %}

<section>
  <div class="container mt-5">
    <div class="row">
      <div class="col-sm-12 col-md-8">
        <h2>{{ page.header }}</h2>
        <p>{{ page.description_long }}</p>
      </div>
      <div class="col-sm-12 col-md-4 list-diensten ">
        <h3>Andere diensten</h3>
        <ul class="list-unstyled">
          {% for dienst in diensten_sorted %}
            <li>
              <a href="{{dienst.url}}">{{ dienst.header }}</a>
            </li>
          {% endfor %}
        </ul>

        {%- comment -%}
          Verbindt de dienstpagina's met de regiopagina's. Zonder deze links
          hangen de regio's alleen aan de footer.
        {%- endcomment -%}
        <h3 class="mt-4">Waar we werken</h3>
        <ul class="list-unstyled">
          {% assign regios = site.regio | sort: 'name' %}
          {% for regio in regios %}
            <li>
              <a href="{{ regio.url | relative_url }}">{{ page.header }} in {{ regio.name }}</a>
            </li>
          {% endfor %}
        </ul>
        <p><a href="{{ '/regio/' | relative_url }}">Bekijk onze volledige werkregio</a></p>
      </div>
    </div>
  </div>
</section>

{% include latest_werk.liquid limit_items="4" %}

{% include cta-banner.liquid %}
