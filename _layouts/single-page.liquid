---
layout: default
image: assets/img/social_schrijnwerkerijtobback.png
---
<section id="sc-hero" class="section-hero div-hero {{page.hero | default: "hero-1"}}">
  <div class="container mt-2">
    <div class="row">
      <div class="col-sm-12">
        <div class="div-hero div-hero-text">
          <div>
            <h1 class="hero">{{ page.header }}</h1>
            <span class="sub-hero">{{ page.subheader }}</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{%- comment -%}
  Breadcrumbs stonden alleen op werk-, dienst- en artikelpagina's, terwijl
  head.liquid op élke pagina BreadcrumbList-JSON-LD uitstuurt. Gestructureerde
  data en zichtbare navigatie liepen daardoor uiteen.
{%- endcomment -%}
{% include breadcrumbs.liquid %}

{{ content }}
